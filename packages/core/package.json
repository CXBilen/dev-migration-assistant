{
  "name": "@devmig/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Core domain: provider contract + registry, project scanner, migration planner, backup/restore engines, path remapper, secret classifier, integrity verifier, job engine. Electron-independent.",
  "exports": {
    ".": "./src/index.ts"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@devmig/archive": "workspace:*",
    "@devmig/model": "workspace:*",
    "@devmig/shared": "workspace:*",
    "zod": "^4.4.3"
  }
}
