{
  "name": "@devmig/model",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Isomorphic domain model (zod schemas + types) shared by renderer, IPC, core and providers. No Node.js imports allowed here.",
  "exports": {
    ".": "./src/index.ts"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}
