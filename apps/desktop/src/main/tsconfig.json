{
  "// purpose": "Nearest tsconfig for the main-process tree: lets ESLint's project service and editors type-check the *.test.ts files that ../../tsconfig.node.json excludes. Type-checking of production code is still driven by ../../tsconfig.node.json.",
  "extends": "../../tsconfig.node.json",
  "compilerOptions": {
    "composite": false,
    "declaration": false,
    "declarationMap": false,
    "emitDeclarationOnly": false,
    "noEmit": true,
    "rootDir": ".",
    "paths": { "@main/*": ["./*"] }
  },
  "include": ["./**/*.ts"],
  "exclude": []
}
