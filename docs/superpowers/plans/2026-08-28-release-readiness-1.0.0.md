# Release readiness — Dev Migration Assistant 1.0.0

Synthesis of three read-only audits (threat-table placeholders, §5 gate checklist, stale docs) against
`/Users/cxbilen/Documents/GitHub/dev-migration-assistant/.claude/worktrees/feat+v0.2-rehydrate`.
Audits ran at `a47a04c`; synthesis re-checked at HEAD `d38db93` (one newer commit: "record a source capability snapshot
in every backup manifest" — adds `packages/core/src/capabilities/snapshot.test.ts`; no effect on any finding).
**In flight at synthesis time:** `packages/core/src/migration.integration.test.ts` carries a 6-line uncommitted addition
(capability-snapshot assertions at ~:296-300) — the other team is mid-edit there; T5's tree-diff assertion (§2, gap G4)
touches the same area, so land it after theirs. Both manifests still read `0.1.0-alpha.6` (`package.json:3`,
`apps/desktop/package.json:3`). Line numbers below are as of `a47a04c`/`d38db93`.

Test evidence: 11 focused vitest runs across the three audits, all green (unit: router, trusted-sender, work-dir,
preload/api, docs-consistency, staging-cache, scoped-fs, redact, errors, log-redaction, merge, git, project-files,
aside-paths, archive, crypto-stream, extract-hardening, handlers, system; integration: restore-engine, backup-engine,
migration, git-provider).

---

## 1. Executive summary

1. **Yes, 1.0.0 can ship after Phase A** — the engines, IPC hardening, fuses, startup sweep and E2E suite are all in the
   tree and tested; nothing in the code is known-broken. What is missing is proof, wording and a human sign-off.
2. **Blocking (code/tests, ~1 day):** four threat-table placeholders have no test at all (T10 hostile file names, T14
   live-pid preflight, T15 logger sink, T9 fuse table) and two are only behaviourally covered (T5 whole-tree
   confinement, T12 JobManager contract). Six small tests close all of them; `tests/docs/docs-consistency.test.ts:412`
   must change in the same commit or CI fails.
3. **Blocking (human):** packaged-app fuse read-back and dev-console check on a `pnpm dist:mac` build, the clean-account
   smoke test, a `.devbackup` from alpha.6 for the compatibility check, and publishing the draft release.
4. **Doc-only (no code risk):** version strings, ROADMAP renumbering (v0.1→1.0, new 1.1 "Rehydrate", v0.2→1.2,
   v0.3→1.3, v1.0→2.0), four "pending" claims in THREAT_MODEL.md for shipped features, three dead rows in
   KNOWN_LIMITATIONS.md, RELEASE.md's E2E and fuse caveats, README/USER_GUIDE alpha copy, an empty CHANGELOG
   `[Unreleased]`.
5. **Sequencing hazard:** the Phase A documentation task (`docs/superpowers/plans/2026-08-28-v0.2-rehydrate-phase-a-foundations.md:2620-2719`)
   is unstarted and edits `CHANGELOG.md`, `KNOWN_LIMITATIONS.md`, `ARCHITECTURE.md`, `DEVBACKUP_SPEC.md`,
   `providers/runtime.md` — land it **before** the 1.0.0 renumbering pass or the two collide in `KNOWN_LIMITATIONS.md`.

---

## 2. Security gate

### 2.1 §5 checklist (`docs/security/THREAT_MODEL.md:105-130`)

| #    | Item (line)                                                                                                                | Status                                               | Evidence                                                                                                                                                                                                                                                                                                                                                                                             | Closes with                                                                                                                                                                                 |
| ---- | -------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| G-1  | Every placeholder names a real passing test (:107)                                                                         | **PARTIAL**                                          | 8 placeholders remain (T5 :92, T6 :93, T9 :96, T10 :97, T12 :99, T13 :100, T14 :101, T15 :102). T6, T13 and two-thirds of T9 are already proven by existing tests that are simply not cited                                                                                                                                                                                                          | Gaps G1–G7 below + doc pass (T10 in §5) + `docs-consistency.test.ts:412` edit (G9)                                                                                                          |
| G-2  | `pnpm verify` + `pnpm verify:e2e` green on CI for the release commit (:109)                                                | **PARTIAL — unverifiable locally**                   | `.github/workflows/ci.yml` runs typecheck→lint→format→unit→integration→build, uploads `apps/desktop/out`, then an `e2e` job runs `pnpm test:e2e`; `package.json` `verify`/`verify:e2e` mirror it. No build output exists in this worktree (`apps/desktop/out/` holds only `types-*`)                                                                                                                 | Human: confirm the green run on the release commit. Doc: `RELEASE.md:89-90` still claims the E2E job fails with no specs — 4 specs exist (`tests/e2e/{smoke,backup,restore,cancel}.e2e.ts`) |
| G-3  | Tamper matrix §5.4 covered (:110)                                                                                          | **DONE**                                             | All 12 rows mapped to `packages/archive/src/archive.test.ts:134,173,202,220,255,274,295,307,337`, `crypto-stream.test.ts:78,84,118,141,155,172`, `header.test.ts:54,74` (58 tests pass)                                                                                                                                                                                                              | Optional: container-level header-transplant and drop-chunk cases in `archive.test.ts › tamper detection` (unit-level exists at `crypto-stream.test.ts:141,172`)                             |
| G-4  | Hostile-archive fixture rejected (:112)                                                                                    | **DONE**                                             | `packages/archive/src/extract-hardening.test.ts:167` (17 table-driven `rejects <case>`), `:172,:210,:224,:280,:286,:342`; `expectRejected` (:46-70) also asserts the temp tree is byte-identical and no symlink was created                                                                                                                                                                          | —                                                                                                                                                                                           |
| G-5  | Hostile-**repository** fixture round-trips or is rejected (:114)                                                           | **PARTIAL**                                          | Branch `-evil`: `git-provider.integration.test.ts:1056`. Parsing: `git.test.ts:40,138,280,287,387`. Unicode name through the container: `packages/archive/src/test-helpers.ts:154`. **No test writes a hostile file name to a real repo** — `packages/test-utils/src/git-fixture.ts:109` `NAME_RE` forbids leading `-`, all fixture files are ASCII (:238-317)                                       | Gap **G7**                                                                                                                                                                                  |
| G-6  | `ScopedFs` escapes + engine-level "nothing outside roots" (:116)                                                           | **PARTIAL**                                          | ScopedFs: `packages/shared/src/scoped-fs.test.ts:17,31,41,54,81`. Engine: `restore-engine.integration.test.ts:409` (escape → `failed`, target absent, `fs.roots` exactly `[newProject, claudeConfigDir, claudeJsonPath]`), `:440` (aside root only for `backup-then-replace`). Provider: `git-provider.integration.test.ts:913`. **No test diffs the whole destination home**                        | Gap **G4**                                                                                                                                                                                  |
| G-7  | IPC layer per ADR-0007, untrusted sender + malformed request rejected (:118)                                               | **DONE**                                             | `apps/desktop/src/main/ipc/router.ts:74,90,109`; `router.test.ts:23,36,69,79,94,118,128`; `trusted-sender.test.ts:18,25,32,40,46,63,73`; `handlers.test.ts:90,114,149,228`; `tests/e2e/smoke.e2e.ts:79`                                                                                                                                                                                              | Cite in T9 cell (doc pass)                                                                                                                                                                  |
| G-8  | `electronFuses` declared; packaged read-back matches ADR-0007; no dev-console warnings; E2E asserts no Node globals (:120) | **PARTIAL**                                          | Declared: `apps/desktop/electron-builder.yml:23-33` (9 fuses, superset of ADR-0007:14). E2E: `smoke.e2e.ts:59`. **Read-back: nothing** — `grep -rn fuses scripts/ .github/ package.json apps/desktop/package.json` is empty; `release.yml:161-167` verifies codesign only. Dev-console check unrecorded. ADR-0007:14 is prose, not the "fuse table" §5 cites                                         | Gaps **G2** (config test) and **G8** (CI read-back step); human: dev-console check                                                                                                          |
| G-9  | Production CSP has no `localhost` (:123)                                                                                   | **DONE (assertion exists; artifact absent locally)** | `apps/desktop/electron.vite.config.ts:13-40` `PRODUCTION_CSP` + build-only `productionCsp()` plugin (`:66`); `smoke.e2e.ts:97` asserts the built `out/renderer/index.html`                                                                                                                                                                                                                           | Optional 5-line unit test on `PRODUCTION_CSP` (no build needed). Note `frame-ancestors` in a `<meta>` CSP is ignored — harmless; real guard is `window.ts:67,71`                            |
| G-10 | `claude-running` decision recorded (:124)                                                                                  | **DONE (decision) / PARTIAL (test)**                 | Decision = warning, recorded at `KNOWN_LIMITATIONS.md:69`, `THREAT_MODEL.md:101`, `providers/claude-code.md:141`, `ROADMAP.md:42`, `USER_GUIDE.md:107,122,142,240-242`; code `plan.ts:176-184` (`status:'warn'`, `blocking:false`). **Warn branch untested** — every test injects `isProcessAlive: () => false` (`merge.test.ts:34`, `provider.integration.test.ts:51`, `empty-sessions.test.ts:29`) | Gap **G3**; tick the box with "decision: warning; blocking variant deferred to 1.2"                                                                                                         |
| G-11 | Startup sweep implemented and tested (:126)                                                                                | **DONE**                                             | `apps/desktop/src/main/services/work-dir.ts` (`:12` prefixes, `:13` 24 h grace, temp-root containment), wired `main/index.ts:102-105`; `work-dir.test.ts:21,38,53,58`                                                                                                                                                                                                                                | Cite in T13 cell (doc pass)                                                                                                                                                                 |
| G-12 | `child_process` grep + ESLint rules (:127)                                                                                 | **PARTIAL — intent met, text stale**                 | Only hit: `apps/desktop/scripts/after-pack.cjs:7` (`execFileSync('codesign', [...])`, build-time). Zero hits in `.ts`. Seam `packages/shared/src/exec.ts:49,60` (`execa`, `shell:false`). `eslint.config.js:48-72` blocks `child_process.exec` / import forms / `eval`                                                                                                                               | Reword :127 (§3). Optional: add selectors for `execSync`, `spawn(...,{shell:true})`, `new Function`                                                                                         |
| G-13 | Diagnostics → Copy has no secret-shaped strings (:129)                                                                     | **DONE**                                             | `system.ts:171-175` `redactObject(await collect())`; `system.test.ts:198` with planted `sk-ant-…` at `:42` asserts `[REDACTED]`; renderer path is the same IPC (`diagnostics.tsx:33`)                                                                                                                                                                                                                | Optional: broaden fixture to `ghp_`, `AKIA`, bearer header, `KEY=value`, JWT, and a `searchPaths` entry (`Diagnostics.searchPaths` added in `425d4c8`)                                      |
| G-14 | Threat rows re-read; residual risks updated (:130)                                                                         | **GAP**                                              | Four false "pending" claims (T9 ×3, T13 ×1) and three unvalidated-but-validated residuals (T5, T6, T15)                                                                                                                                                                                                                                                                                              | Doc pass (§3, THREAT_MODEL.md)                                                                                                                                                              |

### 2.2 Threat-table placeholders (`THREAT_MODEL.md:92-102`)

| Row      | Placeholder                                                       | Status               | Evidence (cite this in the cell)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Action                                                                                       |
| -------- | ----------------------------------------------------------------- | -------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- |
| T5 :92   | engine-level ScopedFs confinement                                 | **PARTIAL**          | `packages/core/src/restore/restore-engine.integration.test.ts › DefaultRestoreEngine (integration, fake providers + fake archive) › confines providers to the approved roots and keeps going when one fails` (:409); `… › adds announced aside paths to the roots only for backup-then-replace decisions` (:440)                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Cite both; add tree-diff assertion (G4) to satisfy §5:116's literal "no file exists outside" |
| T6 :93   | backup VERIFYING phase                                            | **DONE**             | `packages/core/src/backup/backup-engine.integration.test.ts › DefaultBackupEngine (integration, fake providers + fake archive) › produces a verified backup with the expected manifest, payload layout and progress` (:103 — asserts `storedManifest.id === manifest.id` :187, ops `['create','verify']` :188, `ENCRYPTING` before `VERIFYING` :202-205, verify item `done` :212-215)                                                                                                                                                                                                                                                                                                                                                                                                   | Cite. Optional G6 (verify-failure path)                                                      |
| T9 :96   | untrusted sender / malformed request; E2E Node globals; fuse read | **PARTIAL (2 of 3)** | (a) `apps/desktop/src/main/ipc/router.test.ts › IPC router › rejects untrusted senders with a PERMISSION_DENIED envelope and logs it` (:23), `… › rejects payloads that fail the request schema without echoing them` (:36); `trusted-sender.test.ts › createSenderGuard › *` (:18-73). (b) `tests/e2e/smoke.e2e.ts › renderer is sandboxed: no Node globals, window.open denied` (:59), `… › packaged index.html carries a strict CSP without localhost origins` (:97). (c) fuse read: **no test**                                                                                                                                                                                                                                                                                     | Cite (a)+(b); write G2; keep packaged read-back as manual/CI step (G8)                       |
| T10 :97  | hostile-name round trip                                           | **GAP**              | Only parser/argv units (`git.test.ts:40,138,280,287,387`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Write G7                                                                                     |
| T12 :99  | job manager                                                       | **PARTIAL**          | `packages/core/src/migration.integration.test.ts › Definition of Done: Mac A (alice) → Mac B (bob) › Scenario C: cancelling a restore leaves no destination directory behind` (:594 — `jobs.start/cancel/wait`, `final.status === 'cancelled'`, no destination, no `~/.claude/projects`). No `job-manager.test.ts` exists                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Cite Scenario C; write G5                                                                    |
| T13 :100 | engine cleanup + startup sweep                                    | **DONE**             | `apps/desktop/src/main/services/work-dir.test.ts › sweepWorkDir › removes old staging directories with known prefixes and keeps everything else` (:21), `… › refuses to sweep a work dir outside the OS temp dir unless explicitly allowed` (:38), `… › never sweeps the temp root itself` (:58); `backup-engine.integration.test.ts › … › cancelling mid-collect leaves no output and no staging directory` (:222), `… › removes a partial output file when packing fails` (:368); `restore-engine.integration.test.ts › … › executes an approved plan with collision decisions, verifies, reports and cleans staging` (:310); `packages/core/src/restore/staging-cache.test.ts › StagingCache › extracts once per backup file, re-authenticates on reuse and cleans up by refs` (:71) | Cite; strike "(pending)"                                                                     |
| T14 :101 | preflight with a live pid                                         | **GAP**              | Only the pass branch: `provider.integration.test.ts:324`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Write G3                                                                                     |
| T15 :102 | logger sink                                                       | **GAP**              | `packages/shared/src/logger.ts:28-44` has no test; `log-redaction.test.ts:5` covers the Electron transport (`redactLogMessage`), `redact.test.ts:4,52` the primitives — neither exercises `createLogger`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Write G1                                                                                     |

### 2.3 Gap closers, smallest first

**G1 — logger sink (T15).** Create `packages/shared/src/logger.test.ts`, project **unit**.
Setup: `const records: LogRecord[] = []; const log = createLogger(r => records.push(r), { machine: 'A' })`.
Assert: (1) `log.info('using token=sk-ant-api03-abcdefghijklmnopqrstuvwxyz0123456789')` → `records[0].msg` contains
`[REDACTED]`, not `abcdefghijklmnop` (`logger.ts:34`); (2) `log.warn('ctx', { apiKey: '<token>', nested: { password: 'hunter22' } })`
→ `JSON.stringify(records[1].ctx)` contains neither (`logger.ts:36`); (3) `log.child({ jobId: 'job_1', authorization: 'Bearer <token>' }).error('boom')`
→ child base context redacted, `jobId` intact (`logger.ts:43`); (4) `createLogger(sink, {}, 'warn').debug('x')` emits
nothing; `level`/`time` shape per `logger.ts:31-37`; (5) `noopLogger.error('token=…')` calls no sink.

**G2 — fuse table (T9c, §5:120).** Create `apps/desktop/src/main/electron-fuses.test.ts`, project **unit** (glob
`apps/desktop/src/main/**/*.test.ts`). Setup: read `apps/desktop/electron-builder.yml` from disk, parse with the repo's
`yaml` package (precedent: `tests/docs/docs-consistency.test.ts:279 describe('GitHub workflows')`). Assert
`electronFuses.runAsNode === false`, `enableNodeOptionsEnvironmentVariable === false`,
`enableNodeCliInspectArguments === false`, `onlyLoadAppFromAsar === true`, `enableEmbeddedAsarIntegrityValidation === true`
— the ADR-0007 set — plus the remaining four declared values so a silent edit fails CI.

**G3 — live-pid preflight (T14, §5:124).** Add an `it` to `packages/providers/claude-code/src/merge.test.ts` (or sibling
`preflight.test.ts`), project **unit**; reuse `createFakeHome`, `planningContext`, `mapping()`. Setup: ensure
`<home.claudeConfigDir>/sessions/` exists (`plan.ts:172` skips the probe otherwise); write `<uuid>.json` =
`{ "pid": 4242, "cwd": "/Users/alice/Documents/GitHub/demo", "sessionId": "<uuid>" }`, a second `{ "pid": 999999 }`, a
third with malformed JSON. `new ClaudeCodeProvider({ isProcessAlive: (pid) => pid === 4242, platform: 'linux' })`.
Act: `provider.planRestore(input, planningContext(...))`. Assert on `plan.preflight.find(p => p.id === 'claude-running')`:
`status === 'warn'`, `blocking === false`, `label === 'Claude Code not running'`, `detail` contains `'4242'` and
`'1 Claude Code process is running'`, not `'999999'`; no throw on the malformed file. Second case: all dead → `'pass'`.
Optional: assert no code path throws `CLAUDE_RUNNING` (`packages/model/src/errors.ts:27`) to pin the §5:124 decision.

**G4 — whole-tree confinement (T5, §5:116).** ~15 lines in `packages/core/src/restore/restore-engine.integration.test.ts:409`
(or `migration.integration.test.ts`, which already imports `walkFiles` at `:30` — but that file is mid-edit; prefer
restore-engine), project **integration**. Setup: `before = new Set(await walkFiles(b.test.homeDir))` before `execute`,
`after` afterwards. Assert: every path in `after − before` starts with one of `approvedRoots ∪ announcedAsidePaths`.

**G5 — JobManager contract (T12).** Create `packages/core/src/jobs/job-manager.test.ts`, project **unit** (no fs, no git).
Setup: `new JobManager(createLogger(r => records.push(r)))`, runner resolving a deferred promise. Assert against
`job-manager.ts`: per-job `AbortController` (two jobs, cancel one, only that `ctx.signal.aborted`); `cancel()` →
`status:'cancelled'`, `phase:'CANCELLED'`, last `recentEvents` entry `level:'warn'` (`:110-122`); a runner rejecting with
`MigrationError('CANCELLED')` without abort is still `cancelled` (`:109-111`); `cancel()` unknown id → `JOB_NOT_FOUND`,
finished job → `JOB_ALREADY_FINISHED` (`:143-149`); `result()` rethrows serialized code/hint (`:161-169`);
`recentEvents` capped at `MAX_RECENT_EVENTS = 200` (`:24`, `:73-75`); `progress('token=sk-ant-…')` reaches the logger
as `[REDACTED]`.

**G6 (optional) — verify-failure path (T6).** In `backup-engine.integration.test.ts`, project **integration**: make the
fake archive's `verify` op reject with `INTEGRITY_MISMATCH` once; assert the job ends `failed` with that code and the
output path does not exist. Nearest existing case is `:368` (fails at pack, not verify).

**G7 — hostile file names (T10, §5:114).** Project **integration** (real git, `pool:'forks'`, 180 s). Either extend
`packages/test-utils/src/git-fixture.ts` with a `hostileNames` option (relax `NAME_RE` at `:109` only for file entries,
never for the repo dir) or add files after `createGitRepoFixture` via `fs.writeFile` + `git(['add','--','<name>'])`. Add
to `git-provider.integration.test.ts › GitProvider round trip: special repositories` (`:936`) an
`it('round-trips hostile file and branch names without executing them')`. Files: `-rf.txt`, `line\nbreak.txt`,
`qu"ote'.txt`, `back\\slash.txt`, `café.txt` (NFC) + `café-2.txt`, one untracked, one ignored; branch `feat/weird--name`.
NUL is not a valid POSIX filename — its rejection is already `git.test.ts:387`. Act: scan → backup into a `ScopedFs`
staging root → `planRestore` → `restore` onto `/Users/bob/Developer/demo` → `verify`, using the file's harness helpers.
Assert: byte-identical content at every hostile path; `captureGitState`/`compareGitState` (`:43-44`) equivalent; the
NFC/NFD pair produced two files **or** the artifact aborted with an explanation (accept either explicitly); no hook ran
(marker pattern from `:752`); the injected `Exec` saw hostile names only as argv elements after `--`.

**G8 — packaged fuse read-back (§5:120).** Add a step to `.github/workflows/release.yml` after `dist:mac` (near
`:161-167`): `npx @electron/fuses read --app "apps/desktop/release/mac-arm64/Dev Migration Assistant.app"` and fail the
job unless the output shows RunAsNode/NodeOptions/NodeCliInspect **disabled** and OnlyLoadAppFromAsar/EmbeddedAsarIntegrity
**enabled**. `@electron/fuses@1.8.0` is already transitively present (`pnpm-lock.yaml:488`). Cannot be verified in this
worktree (no dist build); the human step in §4 covers the first release.

**G9 — doc guard.** `tests/docs/docs-consistency.test.ts:412` currently `expect(tm).toContain('to be validated by the security gate')`
— it **fails** once the last row placeholder is removed. Replace with a table-scoped negative check:

```ts
const table = tm.slice(
  tm.indexOf('## 4. Threat table'),
  tm.indexOf('## 5. Security gate checklist'),
)
expect(table, 'no unvalidated mitigation may remain in the threat table').not.toContain(
  '<to be validated by the security gate>',
)
```

so the legend (`:83`) and checklist (`:107`) may keep the phrase. Also retitle `:415` `'KNOWN_LIMITATIONS lists the v0.1 gaps the ADRs imply'`
→ `'… the 1.0 gaps …'` (description only). Optional hardening: parse every "Validated by" cell and assert each cited
`file › describe › it` resolves to an existing file and title.

---

## 3. Docs to update for 1.0.0

Pinned phrases that must survive every edit below (plain `toContain` in `tests/docs/docs-consistency.test.ts:377-429`):

- `.github/PULL_REQUEST_TEMPLATE.md` (`:379-385`): `ScopedFs`, `Exec(file, args[])`, `MigrationError`, `pnpm verify`, `CHANGELOG.md`.
- `docs/security/THREAT_MODEL.md` (`:394-409`): `Stolen backup`, `Malicious backup input`, `Path traversal entry`,
  `Symlink / hardlink attack`, `Restore outside the approved destination`, `Corrupted archive`, `Decompression bomb`,
  `Manipulated manifest`, `Renderer compromise`, `Malicious project file names`,
  `Command injection via git branch / path / remote strings`, `Cancellation mid-operation`, `App crash with temp files`,
  `Claude Code running during restore`; and (`:412`, until G9 lands) `to be validated by the security gate`.
- `docs/KNOWN_LIMITATIONS.md` (`:417-426`): `Submodules` (:42), `Stashes` (:43), `Hooks` (:44), `Plugins are reinstalled` (:65),
  `Keychain` (:64), `200 characters` (:66), `No Windows or Linux` (:17 left column), `to be finalized` (:3, only occurrence).
- Version parity `package.json:3` == `apps/desktop/package.json:3` (`docs-consistency.test.ts:272-276`, `release.yml`).
- All ROADMAP links in the docs are file-level (no `#anchors`), so heading renames are safe — re-run the link test
  (`docs-consistency.test.ts:151-172`) anyway.

### `package.json:3`, `apps/desktop/package.json:3`

`"version": "0.1.0-alpha.6"` → `"version": "1.0.0"` (both, same commit).

### `CHANGELOG.md`

- `:9` `## [Unreleased]` is empty. Add `## [1.0.0] - <tag date>` absorbing (a) the unwritten Phase A entries — deterministic
  search-path resolution, `manifest.capabilities` snapshot (`d38db93`), secret-hygiene fixes (MCP `args`/`url`,
  `session-env/` never migrated, credentials stripped from remote URLs) — and (b) the first-stable-release framing. Do not
  rewrite alpha sections (`:60`, `:71`, `:91-110`); they are accurate history.

### `README.md`

| Line     | Current                                                                                                                                                                                                              | Replacement                                                                                                                                                                                      |
| -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| :14      | release badge `?include_prereleases&label=release`                                                                                                                                                                   | drop `include_prereleases`                                                                                                                                                                       |
| :32      | `> **Status:** v0.1.0-alpha (pre-release) · macOS 13+ · Apple Silicon · local-only…`                                                                                                                                 | `> **Status:** 1.0.0 · macOS 13+ · Apple Silicon · local-only, no accounts, no telemetry.`                                                                                                       |
| :33-34   | "The `0.1.0-alpha.1` build is a UI preview on built-in data: it shows a **Preview data** badge and reads or writes nothing on your machine. The bridge to the engines lands in the next alpha — see `CHANGELOG.md`." | delete; replace with `> Create Backup and Restore Backup operate on your real projects and Claude Code data — read-only during backup, and restore writes only to the destinations you approve.` |
| :44      | `<sub>Alpha screenshots.</sub>`                                                                                                                                                                                      | `<sub>Screenshots from the 1.0.0 build.</sub>` **only if** `docs/assets/screenshots/*.png` are re-captured; otherwise `<sub>Screenshots from a development build.</sub>`                         |
| :171     | `**v0.1** targets macOS on Apple Silicon.`                                                                                                                                                                           | `**1.0** targets…`                                                                                                                                                                               |
| :175-178 | provider Status `✓ v0.1` (×4)                                                                                                                                                                                        | `✓ 1.0`                                                                                                                                                                                          |
| :179-181 | `Codex CLI / Cursor / VS Code — planned — v0.2`                                                                                                                                                                      | `1.2`                                                                                                                                                                                            |
| :182     | `Ghostty — planned — after v0.2`                                                                                                                                                                                     | `1.3` (ROADMAP already lists it under the v0.3 block)                                                                                                                                            |
| :183     | `Homebrew — planned — v0.3`                                                                                                                                                                                          | `1.3`                                                                                                                                                                                            |
| :185     | `Known gaps for v0.1 are tracked honestly in …`                                                                                                                                                                      | `Known gaps for 1.0 …`                                                                                                                                                                           |
| :198     | `**Alpha builds are ad-hoc signed, not notarized.**`                                                                                                                                                                 | `**Release builds are ad-hoc signed, not notarized.**`                                                                                                                                           |
| :204-205 | `(very early alpha builds were completely unsigned)`                                                                                                                                                                 | `(a download that lost its signature)`                                                                                                                                                           |
| :290     | `[Roadmap](docs/ROADMAP.md) \| v0.2 / v0.3 / v1 scope`                                                                                                                                                               | `1.1 / 1.2 / 1.3 / 2.0 scope`                                                                                                                                                                    |
| :291     | `[Known limitations] \| What v0.1 does not do`                                                                                                                                                                       | `What 1.0 does not do`                                                                                                                                                                           |

### `docs/USER_GUIDE.md`

| Line   | Current                                                                                                                                                                  | Replacement                                                                                                                                           |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| :8     | `> Written against the v0.1 design. Screen names may differ slightly until the UI is final;`                                                                             | `> Written against the 1.0 release. Anything described as "the app checks" or "the app refuses" is enforced in the core engines, not just in the UI.` |
| :10-12 | "Alpha builds that ship before the Electron bridge to the engines lands run the UI on built-in preview data and show a **Preview data** badge — … (see `CHANGELOG.md`)." | delete                                                                                                                                                |
| :67    | `**There is no recovery in v0.1**`                                                                                                                                       | `**There is no password recovery in 1.0**`                                                                                                            |
| :111   | `v0.1 builds are unsigned, so on first launch use right-click → **Open**`                                                                                                | `1.0 builds are ad-hoc signed but not notarized, so on first launch use right-click → **Open**`                                                       |
| :171   | `# expected to be empty: stashes are not migrated in v0.1`                                                                                                               | `… in 1.0`                                                                                                                                            |
| :226   | `Expected for v0.1 (unsigned).`                                                                                                                                          | `Expected for 1.0 (not notarized).`                                                                                                                   |
| :232   | `There is no recovery key in v0.1.`                                                                                                                                      | `… in 1.0.`                                                                                                                                           |
| :244   | `In v0.1 the check is a warning, not a hard stop`                                                                                                                        | `In 1.0 the check is a warning, not a hard stop`                                                                                                      |

### `docs/KNOWN_LIMITATIONS.md` (land the Phase A doc task here first — it adds a `session-env` row and rewords :88)

| Line | Current                                                                                                       | Replacement                                                                                                                                                                                                                                                                                           |
| ---- | ------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| :1   | `# Known limitations (v0.1)`                                                                                  | `# Known limitations (1.0)`                                                                                                                                                                                                                                                                           |
| :3   | `**Status: to be finalized before the v0.1 release.**`                                                        | `**Status: finalized for the 1.0.0 release.** Every row was confirmed against the shipped code before tagging; a row added after a tag is marked _to be finalized_ until the next release confirms it.` (keeps the pin)                                                                               |
| :16  | `` `pnpm dist:mac:universal` exists but is untested; a universal build is on the roadmap for v0.2.``          | `` `pnpm --filter @devmig/desktop dist:mac:universal` exists but is untested; a universal build is on the [roadmap](ROADMAP.md) for 1.2.`` (the root script does not exist — `package.json:17-33`; it lives at `apps/desktop/package.json:20`)                                                        |
| :17  | `…a read-only restore tool for other platforms is a v1 idea at most.`                                         | `…is a 2.0 idea at most.` (left column `No Windows or Linux` unchanged)                                                                                                                                                                                                                               |
| :18  | `**Unsigned, un-notarized DMG** \| gap \| Gatekeeper requires right-click → Open.`                            | `Ad-hoc signed, not notarized \| gap \| Gatekeeper offers "Open Anyway" / right-click → Open. Signing is wired in `release.yml` and only needs certificate secrets ([RELEASE.md](release/RELEASE.md)).`                                                                                               |
| :19  | `**Electron fuses not yet declared** \| gap \| …`                                                             | **delete** (shipped: `electron-builder.yml:23-33`). Optional replacement in the Desktop-app section: "Packaged builds flip `RunAsNode`/`NodeOptions`/`NodeCliInspect` off and `OnlyLoadAppFromAsar` on; verify with `npx @electron/fuses read --app …`."                                              |
| :20  | `**Alpha builds may run the UI on preview data** \| gap \| …`                                                 | **delete**, or narrow to dev-only in the Desktop-app section: `In `electron-vite dev` without a complete preload bridge the renderer falls back to in-memory mock data and shows a "Preview data" badge; packaged builds always use the real bridge.`                                                 |
| :22  | `…but v0.1 ships no CLI binary.`                                                                              | `…but 1.0 ships no CLI binary.`                                                                                                                                                                                                                                                                       |
| :29  | `…A recovery key … is planned for v0.2.`                                                                      | `…planned for 1.2.`                                                                                                                                                                                                                                                                                   |
| :30  | `No compression … v0.1 stores the tar stream uncompressed…`                                                   | `…1.0 stores…`; append `(1.2)` to "Optional gzip inside the encryption is planned"                                                                                                                                                                                                                    |
| :70  | retention row Notes                                                                                           | end with `…raise `cleanupPeriodDays` before the first launch — this is guidance in the [user guide](USER_GUIDE.md#6-re-authenticate); the app does not warn about it.` (no retention code path exists — only hit is a fixture assertion at `provider.integration.test.ts:580`)                        |
| :85  | `` `.vscode/`, `.idea/` and similar are not offered in v0.1…``                                                | `…not offered in 1.0 (VS Code / Cursor providers are on the roadmap for 1.2).`                                                                                                                                                                                                                        |
| :96  | `…— to be validated end to end before release.`                                                               | `…— validated end to end by `packages/core/src/restore/restore-engine.integration.test.ts › DefaultRestoreEngine (integration, fake providers + fake archive) › adds announced aside paths to the roots only for backup-then-replace decisions`.`                                                     |
| :125 | `` - `backup-then-replace` aside paths for repositories — validated by: `<to be filled by the integrator>` `` | `…— validated by: `packages/core/src/restore/restore-engine.integration.test.ts › … › adds announced aside paths to the roots only for backup-then-replace decisions`, `packages/core/src/restore/aside-paths.test.ts › … › approves sibling asides and paths inside roots; rejects everything else`` |
| :126 | `` - Retention warning for old sessions — validated by: `<to be filled by the integrator>` ``                 | **delete the bullet** — do not invent a test name                                                                                                                                                                                                                                                     |
| :128 | `## Desktop app (v0.1 alpha)`                                                                                 | `## Desktop app`                                                                                                                                                                                                                                                                                      |

### `docs/ROADMAP.md` — ready-to-paste outline

Replace the headings at `:7`, `:16`, `:47`, `:59` and apply the marked line edits; bodies otherwise unchanged.

```markdown
## 1.0 — the core loop (current)

- macOS 13+, Apple Silicon, ad-hoc signed DMG (not notarized).
- Providers: Claude Code, Git, project files, runtime facts.
- Encrypted `.devbackup` (Argon2id + chunked AES-256-GCM), streaming everywhere, hardened extraction.
- Restore plan with collisions, preflight, path-remap report; write only through `ScopedFs`.
- Metadata-driven Claude Code project matching; schema-owned path rewriting only.
- Deterministic search-path resolution (launchd PATH + `/etc/paths(.d)` + well-known user tool dirs); every `Exec`
  call receives it. No login shell is spawned.
- Capability snapshot in the manifest (`manifest.capabilities`): tool versions, resolved paths and install method, plus
  the Claude Code versions that wrote the transcripts — names and paths only.
- Secret-hygiene fixes: MCP `args`/`url` secrets classified sensitive, `session-env/` never migrated, credentials
  stripped from git remote URLs.
- Electron main/preload bridge, typed zod-validated IPC, packaged-build fuses, startup sweep of leftover staging
  directories, Playwright E2E suite.
- Definition of done: fake Mac A → fake Mac B with a changed username and project path, `claude --resume` works.

## 1.1 — Rehydrate: post-restore bootstrap

- **Post-Restore Bootstrap Engine** — turns "restore completed" into "development environment ready": consent-gated,
  terminal-hosted remediation actions built from the capability snapshot (install missing tools, re-authenticate,
  reinstall plugins). Credentials are still never captured or migrated; the app proposes, the user approves, nothing
  runs silently.

## 1.2 — more tools, smoother installs

(body of the current `v0.2` block, `:18-45`, with two edits)

- :42 → `- Blocking `CLAUDE_RUNNING` preflight (1.0 and 1.1 warn — the Rehydrate spec keeps it a warning through 1.1).`
- :43 → `- Larger E2E matrix: collision policies and a tampered backup file.` (cancellation `tests/e2e/cancel.e2e.ts:27` and wrong password `tests/e2e/restore.e2e.ts:48` shipped)

## 1.3 — the rest of the machine

(body of the current `v0.3` block, `:49-57`; **decision needed:** Homebrew provider `:49-50` and Node runtimes `:51-52`
overlap the 1.1 Rehydrate scope and the shipped capability snapshot — move them up to 1.1 or leave them here)

## 2.0 — Mac → Mac direct transfer

(body of the current `v1.0` block, `:61-67`, with one edit)

- :67 → `- Stable `formatVersion` and a documented compatibility policy (signed releases are already the default from 1.2).`

## Explicitly not planned

(unchanged)
```

### `docs/release/RELEASE.md`

| Line   | Current                                                                                                                                                                                 | Replacement                                                                                                                                                                                                                                                 |
| ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| :16-17 | `App versions before `1.0.0`may bump the minor for breaking UI/CLI changes; the container format must remain readable across`0.x`releases once`0.1.0` ships.`                           | `From `1.0.0`the app version follows SemVer strictly: breaking UI/CLI changes bump the major. Every`1.x`reader must keep opening containers written by any earlier`1.x`.`                                                                                   |
| :45-47 | (signing/notarization as a later item)                                                                                                                                                  | keep — accurate                                                                                                                                                                                                                                             |
| :66-70 | `…are flipped by electron-builder after packaging and before signing once the `electronFuses`block is declared in`apps/desktop/electron-builder.yml` (pending; see the security gate…)` | `…are flipped by electron-builder after packaging and before signing; the `electronFuses`block is declared in`apps/desktop/electron-builder.yml`. Verify a release build with `npx @electron/fuses read --app …`(automated in`release.yml` once G8 lands).` |
| :72-73 | `Pre-release versions (`0.1.0-alpha.1`, `0.1.0-beta.2`, …) … the tag is `v0.1.0-alpha.1``                                                                                               | `Pre-release versions (`1.1.0-alpha.1`, `1.1.0-beta.2`, …) … the tag is `v1.1.0-alpha.1``                                                                                                                                                                   |
| :89-90 | `(the E2E job fails while tests/e2e has no *.e2e.ts files — Playwright exits non-zero on "no tests found")`                                                                             | delete the parenthetical; also drop `--pass-with-no-tests` from `package.json:29` `test:e2e` so a missing spec fails loudly                                                                                                                                 |
| :95    | `install DMG (unsigned flow from the README)`                                                                                                                                           | `install DMG (ad-hoc-signed flow from the README)`                                                                                                                                                                                                          |
| §4     | —                                                                                                                                                                                       | add a manual line: `- [ ] `pnpm dev` console shows no Electron security warnings (record the Electron version checked).`                                                                                                                                    |

### `docs/security/THREAT_MODEL.md`

| Line                  | Current                                                                                                                                                                                                           | Replacement                                                                                                                                                                                                                         |
| --------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| :5-7                  | "Rows marked **to be validated by the security gate** describe mitigations that are designed (ADR-backed) but whose implementation or test was still in flight when this document was last aligned with the code" | `Every row's **Validated by** cell names a passing test at the tagged release commit; the 1.0.0 security gate (§5) was walked on <date>.`                                                                                           |
| :83                   | `` `<to be validated by the security gate>` marks a mitigation whose test (or code) has not landed yet.``                                                                                                         | `` `<to be validated by the security gate>` may appear in a row only between releases, for a mitigation whose test has not landed yet; the §5 gate removes every occurrence before a tag.`` (keeps the phrase for the pin until G9) |
| :88 T1 Residual       | `No recovery key in v0.1`                                                                                                                                                                                         | `No recovery key in 1.0`                                                                                                                                                                                                            |
| :92 T5 Validated by   | `engine-level: `<to be validated by the security gate>` (…)`                                                                                                                                                      | cite the two `restore-engine.integration.test.ts` titles from §2.2 (+ the G4 tree-diff title once written)                                                                                                                          |
| :93 T6 Residual       | `Backup-engine VERIFYING phase: `<to be validated by the security gate>` (…)`                                                                                                                                     | move to Validated by: the `backup-engine.integration.test.ts:103` title; residual keeps "A corrupted backup is unrecoverable by design. Keep two copies."                                                                           |
| :94 T7                | `v0.1 payloads are never compressed…`                                                                                                                                                                             | `1.0 payloads are never compressed…`                                                                                                                                                                                                |
| :96 T9 Where          | `apps/desktop/src/main/index.ts (window posture, navigation lockdown — IPC controllers and sender validation pending)`                                                                                            | `apps/desktop/src/main/index.ts (window posture, navigation lockdown), apps/desktop/src/main/ipc/{trusted-sender,router}.ts, apps/desktop/src/main/ipc/handlers/index.ts`                                                           |
| :96 T9 Where          | `apps/desktop/src/preload/index.ts (pending: currently exposes `meta` only)`                                                                                                                                      | `apps/desktop/src/preload/index.ts (exposes exactly `window.devMigration` — backups, jobs, meta, projects, restore, system)`                                                                                                        |
| :96 T9 Validated by   | the placeholder                                                                                                                                                                                                   | the `router.test.ts`, `trusted-sender.test.ts`, `smoke.e2e.ts:59/97` titles from §2.2 + `apps/desktop/src/main/electron-fuses.test.ts › electron-builder fuses › <G2 title>`                                                        |
| :96 T9 Residual       | `Fuses are not yet declared (§5).`                                                                                                                                                                                | `Fuses are declared in `electron-builder.yml`; the packaged-app `@electron/fuses read`runs in`release.yml` (and manually for the first 1.0.0 build).`                                                                               |
| :99 T12 Validated by  | `job manager: `<to be validated by the security gate>``                                                                                                                                                           | `packages/core/src/migration.integration.test.ts › … › Scenario C: cancelling a restore leaves no destination directory behind`; `packages/core/src/jobs/job-manager.test.ts › JobManager › <G5 titles>`                            |
| :100 T13 Where        | `startup sweep in `apps/desktop/src/main` (pending)`                                                                                                                                                              | `startup sweep `apps/desktop/src/main/services/work-dir.ts` (`sweepWorkDir`, wired in `main/index.ts`)`                                                                                                                             |
| :100 T13 Validated by | `engine cleanup and startup sweep: `<to be validated by the security gate>``                                                                                                                                      | the `work-dir.test.ts`, `backup-engine.integration.test.ts:222/368`, `restore-engine.integration.test.ts:310`, `staging-cache.test.ts:71` titles from §2.2                                                                          |
| :101 T14              | `(v0.1 does not block; the `CLAUDE_RUNNING` error code is reserved for a blocking variant)` … `Gate decision: §5.`                                                                                                | `(1.0 does not block; …)`; replace the placeholder with the G3 title; replace `Gate decision: §5.` with `Gate decision (1.0.0): remains a warning; blocking variant planned for 1.2.`                                               |
| :102 T15 Validated by | `logger sink: `<to be validated by the security gate>``                                                                                                                                                           | `packages/shared/src/logger.test.ts › createLogger › <G1 titles>`; `apps/desktop/src/main/services/log-redaction.test.ts › redactLogMessage › redacts secrets in strings, objects and errors while keeping other values`            |
| :120 §5               | `matches the fuse table in ADR-0007`                                                                                                                                                                              | `matches the `electronFuses`block in`apps/desktop/electron-builder.yml` (ADR-0007 names the required subset)` — or add a real table to `docs/architecture/adr/0007-electron-security-posture.md:14` listing all 9 fuses             |
| :127 §5               | `shows only `packages/shared/src/exec.ts`-approved usage`                                                                                                                                                         | `shows only `apps/desktop/scripts/after-pack.cjs` (`execFileSync('codesign', [...])`, build-time, never shipped); all runtime subprocesses go through `packages/shared/src/exec.ts`(execa,`shell: false`)`                          |
| :107-130              | all `- [ ]`                                                                                                                                                                                                       | tick each with a one-line evidence pointer once the corresponding row in §2.1 is DONE                                                                                                                                               |

### `docs/architecture/adr/0007-electron-security-posture.md:14`

Prose sentence naming four fuses → a table of the nine fuses in `electron-builder.yml:23-33` with the expected value,
so §5:120 has a "fuse table" to match against.

### Optional, not user-facing

- `apps/desktop/src/renderer/src/screens/home.tsx:33` badge `Preview data — the native bridge is not connected` renders
  only when `isMockApi()` (`api/index.ts:26-39`), i.e. dev/component tests. Optionally retitle to
  `Development mode — the native bridge is not connected`.
- `docs/architecture/ARCHITECTURE.md`: no stale status remarks (grep for status/pending/alpha empty). Phase A doc task
  adds a search-path paragraph — nothing else.
- `.github/ISSUE_TEMPLATE/*`: no hard-coded versions. Nothing to change.

### Spec/plan files — list only, do not edit

`docs/superpowers/specs/2026-08-28-v0.2-rehydrate-design.md` (`:1,3,7,11,14,15,33,37,61,265,274,289,396-398,410,421,427`)
and `docs/superpowers/plans/2026-08-28-v0.2-rehydrate-phase-a-foundations.md` (`:1,20,26,81,85,1150,1206,1325,1463,2302,2627,2664,2668,2691,2704,2765,2776`)
speak of "v0.2"/"0.2.0-alpha.1". They are dated design records; leave them. If a mapping note is wanted, add one line at
the top of each: `Numbering note: "v0.1" here is the 1.0 release; "v0.2 Rehydrate" ships as 1.1 (first pre-release tag v1.1.0-alpha.1).`
`:1150` (`appVersion: '0.2.0'`) is a test-fixture literal inside the plan — it is code, not prose.

---

## 4. Items only a human can do

1. **Packaged-app fuse read-back + dev-console check.** Run `pnpm dist:mac` on this branch, then
   `npx @electron/fuses read --app "apps/desktop/release/mac-arm64/Dev Migration Assistant.app"` and confirm
   RunAsNode / EnableNodeOptionsEnvironmentVariable / EnableNodeCliInspectArguments are **disabled** and
   OnlyLoadAppFromAsar / EnableEmbeddedAsarIntegrityValidation are **enabled**. Separately run `pnpm dev` once and confirm
   the DevTools console shows no "Electron Security Warning" lines. Paste both outputs into the release PR.
2. **Clean-account smoke test** (`RELEASE.md:95-96`). On a second macOS user account: install the DMG (right-click → Open),
   create a backup of a fixture project, restore into a different path, confirm `claude --resume` finds the sessions and
   `git status` matches the source. Needs a GUI login and a real `claude` binary — not scriptable.
3. **Compatibility fixture** (`RELEASE.md:97`). Produce one `.devbackup` with the published `0.1.0-alpha.6` build (known
   password, tiny fixture project) and hand it over; only then can "backups from the previous release still open" be a test
   instead of a checkbox. Today no such fixture exists under `fixtures/`.
4. **CI green on the release commit** (`RELEASE.md:89`, gate :109). Confirm `pnpm verify` and `pnpm verify:e2e` passed on
   the exact SHA you tag.
5. **Gatekeeper on the downloaded DMG** (`RELEASE.md:98-99`). Download the artifact through a browser so the quarantine
   attribute is set, open it, confirm the right-click → Open flow works.
6. **Sign-off and publish** (`RELEASE.md:91-93, :100`). Tick the 14 §5 boxes (§2.1 gives the evidence for each), push the
   `v1.0.0` tag, review the draft release, publish, announce. Two §5 items are judgement calls, not tests: the
   `claude-running` decision (:124 — keep as warning) and "threat rows re-read" (:130).
7. **Screenshots.** Decide whether `docs/assets/screenshots/*.png` are re-captured from the 1.0.0 build; the README caption
   depends on it (§3).
8. **Roadmap decision.** Whether Homebrew provider and Node-runtime manifests move from 1.3 up to 1.1 Rehydrate (§3 outline).

---

## 5. Proposed task list — `release-1.0.0`

| #   | Task                                                                                                                                                                                                                 | Files                                                                                                                                                                                                                     | Test                                               | Acceptance                                                                                                                                                                                  |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T1  | Land the Phase A documentation task first (prerequisite; owned by the rehydrate team)                                                                                                                                | `CHANGELOG.md`, `docs/KNOWN_LIMITATIONS.md`, `docs/architecture/ARCHITECTURE.md`, `docs/backup-format/DEVBACKUP_SPEC.md`, `docs/providers/runtime.md`, `packages/core/src/providers/contract.ts:46` per plan `:2620-2719` | `tests/docs/docs-consistency.test.ts` (unit)       | Plan checkboxes ticked; docs test green; pinned phrases intact                                                                                                                              |
| T2  | Logger sink test (G1)                                                                                                                                                                                                | new `packages/shared/src/logger.test.ts`                                                                                                                                                                                  | itself (unit)                                      | Message, ctx and child-base secrets arrive at the sink as `[REDACTED]`; level filter and noop verified                                                                                      |
| T3  | Fuse config test (G2)                                                                                                                                                                                                | new `apps/desktop/src/main/electron-fuses.test.ts`                                                                                                                                                                        | itself (unit)                                      | Asserts all 9 `electronFuses` values in `electron-builder.yml`; editing one fails CI                                                                                                        |
| T4  | Live-pid preflight test (G3)                                                                                                                                                                                         | `packages/providers/claude-code/src/merge.test.ts` (or new `preflight.test.ts`)                                                                                                                                           | itself (unit)                                      | `claude-running` is `warn`/non-blocking naming the live pid, ignores dead pids and malformed registry files; `pass` when none alive                                                         |
| T5  | Whole-tree confinement assertion (G4) — after the other team's `migration.integration.test.ts` edit lands                                                                                                            | `packages/core/src/restore/restore-engine.integration.test.ts:409`                                                                                                                                                        | itself (integration)                               | Set of files added under the fake home ⊆ approved roots ∪ announced aside paths                                                                                                             |
| T6  | JobManager unit test (G5)                                                                                                                                                                                            | new `packages/core/src/jobs/job-manager.test.ts`                                                                                                                                                                          | itself (unit)                                      | Per-job abort, cancelled-vs-failed classification, `JOB_NOT_FOUND`/`JOB_ALREADY_FINISHED`, 200-event cap, redacted progress                                                                 |
| T7  | Hostile-name fixture + round trip (G7)                                                                                                                                                                               | `packages/test-utils/src/git-fixture.ts`, `packages/providers/git/src/git-provider.integration.test.ts:936`                                                                                                               | itself (integration, real git)                     | `-rf.txt`, newline, quote, backslash, NFC/NFD names survive scan→backup→restore→verify byte-identical or abort with an explanation; no hook runs; names only ever appear as argv after `--` |
| T8  | Backup verify-failure path (G6, optional)                                                                                                                                                                            | `packages/core/src/backup/backup-engine.integration.test.ts`                                                                                                                                                              | itself (integration)                               | A rejected `verify` op yields a `failed` job with `INTEGRITY_MISMATCH` and no output file                                                                                                   |
| T9  | Hardening extras (optional): `PRODUCTION_CSP` unit test; broaden diagnostics fixture (`ghp_`, `AKIA`, bearer, `KEY=value`, JWT, `searchPaths`); ESLint selectors for `execSync`/`spawn({shell:true})`/`new Function` | `apps/desktop/electron.vite.config.ts` test, `apps/desktop/src/main/services/system.test.ts:42`, `eslint.config.js:48-72`                                                                                                 | unit                                               | Each pattern regresses to a failing test/lint                                                                                                                                               |
| T10 | THREAT_MODEL doc pass + doc guard (G9)                                                                                                                                                                               | `docs/security/THREAT_MODEL.md:5,83,88,92-102,107-130`, `docs/architecture/adr/0007-electron-security-posture.md:14`, `tests/docs/docs-consistency.test.ts:412,415`                                                       | docs-consistency (unit)                            | Zero `<to be validated by the security gate>` inside the threat table; every "Validated by" title exists (T2–T7 titles); test :412 is the table-scoped negative check                       |
| T11 | Renumber and de-alpha user docs                                                                                                                                                                                      | `README.md`, `docs/USER_GUIDE.md`, `docs/KNOWN_LIMITATIONS.md`, `docs/ROADMAP.md`, `docs/release/RELEASE.md` per §3                                                                                                       | docs-consistency (unit, incl. link test :151-172)  | No `v0.1`/`alpha`/`pending` in shipped-feature copy; ROADMAP headings 1.0/1.1/1.2/1.3/2.0; all 8 KNOWN_LIMITATIONS pins present                                                             |
| T12 | Version bump + CHANGELOG `[1.0.0]`                                                                                                                                                                                   | `package.json:3`, `apps/desktop/package.json:3`, `CHANGELOG.md:9`                                                                                                                                                         | docs-consistency `:272-276` (unit)                 | Both manifests `1.0.0`; `[1.0.0] - <date>` lists Phase A + first-stable framing; `[Unreleased]` empty                                                                                       |
| T13 | Packaged fuse read-back in CI (G8)                                                                                                                                                                                   | `.github/workflows/release.yml` (~`:161-167`)                                                                                                                                                                             | workflow run on the tag (human confirms first run) | Job fails when any of the 5 ADR-0007 fuses differs from the declared value                                                                                                                  |
| T14 | Human gate (§4 items 1–8)                                                                                                                                                                                            | —                                                                                                                                                                                                                         | —                                                  | All 14 §5 boxes ticked with evidence in the release PR; draft release published                                                                                                             |

Dependency order: T1 → (T2, T3, T4, T6, T7, T8, T9 in parallel; T5 after the in-flight edit) → T10 → T11 → T12 → T13 → T14.
