# Threat model

**Scope:** Dev Migration Assistant v0.1 — the macOS Electron app, the `.devbackup` container, and the restore path.
**Status:** living document. Rows marked **to be validated by the security gate** describe mitigations that are
designed (ADR-backed) but whose implementation or test was still in flight when this document was written; the
integrator fills in the `validated by` test names before a release.

Related: [ADR-0003 container](../architecture/adr/0003-devbackup-container.md) ·
[ADR-0006 Git](../architecture/adr/0006-git-portable-state.md) ·
[ADR-0007 Electron](../architecture/adr/0007-electron-security-posture.md) ·
[ADR-0008 restore transactions](../architecture/adr/0008-restore-transactions-and-collisions.md) ·
[research: archive crypto](../research/archive-crypto.md) · [research: Electron security](../research/electron-security.md).

## 1. Assets

| Asset                                                                          | Why it matters                                                                                   |
| ------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------ |
| Source code, uncommitted changes, untracked files                              | Unpublished work; may be proprietary.                                                            |
| Claude Code transcripts, memory, history, checkpoints                          | Contain prompts, tool output and file contents; may echo secrets (tokens pasted, `.env` cat'ed). |
| Env files, MCP `env`/`headers` blocks, local settings                          | Frequently hold API keys and tokens.                                                             |
| Credentials (Keychain items, `oauthAccount` in `~/.claude.json`, session keys) | Grant access to accounts. **Never migrated.**                                                    |
| The backup password                                                            | The only secret protecting a `.devbackup` at rest.                                               |
| Integrity of the user's existing data on both machines                         | Backup must never mutate the source; restore must never write outside what the user approved.    |
| The user's machine (code execution, filesystem)                                | A malicious backup or project must not be able to run code or write arbitrary files.             |

## 2. Trust boundaries

```text
 untrusted ───────────────────────────────────────────────────────────────────────── trusted
  ┌──────────────┐  typed IPC   ┌──────────────┐   Exec(args[])   ┌──────────────┐
  │  Renderer    │ ───────────▶ │  Main        │ ───────────────▶ │ git / open   │
  │  (sandboxed) │ ◀─────────── │  (privileged)│                  └──────────────┘
  └──────────────┘  envelopes   │  + Core      │   ScopedFs       ┌──────────────┐
                                │              │ ───────────────▶ │ approved     │
  ┌──────────────┐  zod +       │              │                  │ destinations │
  │ .devbackup   │ ───────────▶ │  Archive     │                  └──────────────┘
  │ (untrusted)  │  hardened    └──────────────┘
  └──────────────┘  extraction         ▲ read-only
  ┌──────────────┐                     │
  │ project      │ ────────────────────┘  file names, branch names, remotes are DATA
  │ content      │
  └──────────────┘
```

1. **Renderer → Main.** The renderer is untrusted (ADR-0007): `sandbox: true`, `contextIsolation: true`,
   `nodeIntegration: false`, strict CSP, no `ipcRenderer` exposed. Every request crosses a whitelisted channel
   declared in `packages/ipc-contracts/src/channels.ts` with a zod request schema; main validates the sender frame and
   returns errors as values (`IpcEnvelope`), never throwing across the bridge.
2. **Backup file → Archive/Core.** On restore the `.devbackup` is hostile input: header bounds are checked before any
   crypto, the manifest is zod-validated before any other entry is accepted, tar entries are filtered, limits
   enforced, checksums verified, and extraction only ever targets a fresh private staging directory.
3. **Providers → filesystem.** Providers never receive raw `fs`. Backup gives them a `ScopedFs` rooted at their
   staging subdirectory; restore gives them a `ScopedFs` whose roots are exactly the destinations approved in the
   plan (`packages/shared/src/scoped-fs.ts`). Reads are unrestricted; every write checks the canonical target and the
   real path of its nearest existing ancestor.
4. **Project content → subprocesses.** File names, branch names, remote URLs and worktree paths found in a project
   are data. They reach `git` only as validated argv elements through `Exec` (`packages/shared/src/exec.ts`); there
   is no code path that builds a shell string.
5. **App → OS.** `dialog.showOpenDialog`, `shell.openExternal` (https only), `open -b com.apple.Terminal <dir>` run in
   main on canonicalised paths; the renderer only ever receives selected paths.

## 3. Attacker models

| Id  | Attacker                                | Capabilities                                                                                                                           | Goal                                                                                |
| --- | --------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- |
| A1  | **Stolen `.devbackup`**                 | Has the file (lost SSD, shared drive, leaked cloud folder); unlimited offline compute; no password.                                    | Read code, transcripts, env files.                                                  |
| A2  | **Malicious / tampered backup**         | Delivers a crafted or modified `.devbackup` to the user (phishing, "here is my project", corrupted transfer).                          | Write files outside the chosen destination, execute code, crash or exhaust the app. |
| A3  | **Malicious project content**           | Controls a repository the user selects for backup or restores: file names, branch names, remote URLs, worktree paths, `.claude` files. | Inject arguments into `git`, escape staging, plant files elsewhere.                 |
| A4  | **Compromised renderer**                | Achieves JS execution in the renderer (XSS via rendered transcript text, dependency compromise).                                       | Reach Node, the filesystem, `git`, or unapproved destinations through IPC.          |
| A5  | **Local attacker with temp-dir access** | Another local user or process that can read/write `os.tmpdir()` while a job runs or after a crash.                                     | Read staged plaintext, swap staged files before they are written (TOCTOU).          |
| A6  | **Environmental failure**               | Cancellation mid-operation, crash, disk full, Claude Code running while restore writes to `~/.claude`.                                 | Leave the user with corrupted, half-written or lost data.                           |

Explicitly **out of scope**: an attacker who already runs as the user with the app's privileges (they can read
`~/.claude` directly), weak user-chosen passwords beyond the enforced minimum, and vulnerabilities in Claude Code,
Git or macOS themselves.

## 4. Threat table

Legend — **Where:** file paths per the architecture (files listed as planned in `packages/*/src/index.ts` may still
be landing). **Validated by:** the test that demonstrates the mitigation; placeholders are marked
`<to be validated by the security gate>`.

| #   | Threat                                                                                           | Attacker | Mitigation                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Where implemented                                                                                                                                                                                          | Validated by                                                                                                                                                                                                                                | Residual risk                                                                                                                                                                                                                |
| --- | ------------------------------------------------------------------------------------------------ | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T1  | **Stolen backup** (offline password guessing, reading contents)                                  | A1       | Encryption by default. Password → KEK with **Argon2id** (hash-wasm; default m = 64 MiB, t = 3, p = 4 (RFC 9106 second recommended option), 16-byte random salt per file; NFC-normalised password). Random 32-byte master key wrapped with AES-256-GCM; content key derived with HKDF-SHA-256. Payload encrypted in fixed-size AES-256-GCM chunks. Header contains no labels, names or manifest — only KDF parameters and the wrapped key. Password minimum 8 characters enforced by the `BackupRequest` schema; UI shows strength guidance.                                                                                                                                                                                                       | `packages/archive/src/create.ts`, `packages/archive/src/types.ts`, `packages/model/src/backup.ts` (`BackupRequest.password.min(8)`)                                                                        | `packages/archive/src/*.test.ts` › encrypt/decrypt round trip, wrong password → `ARCHIVE_AUTH_FAILED` — `<to be validated by the security gate>`                                                                                            | A weak password (dictionary word, short pattern) is crackable in hours to weeks on one GPU; no KDF fixes that. Header metadata (creation time, app version, approximate size) is visible without a password.                 |
| T2  | **Malicious backup input** (crafted header/payload)                                              | A2       | Reader validates the header before any crypto: magic, `formatVersion` (unknown → `ARCHIVE_UNSUPPORTED_VERSION`), header length ≤ 64 KiB, KDF parameter bounds (m 8 MiB–1 GiB, t 1–16, p 1–16, chunk size 4 KiB–16 MiB) so a hostile header cannot demand 4 GiB of RAM, wrapped key exactly 48 bytes. Wrong KEK fails at key unwrap before any payload is read. Per-chunk AAD = `SHA-256(header)                                                                                                                                                                                                                                                                                                                                                   |                                                                                                                                                                                                            | chunk index` binds every chunk to its header and position, so transplanting a payload under another header, reordering or truncating chunks fails at the first affected chunk. Chunk plaintext is only released after its GCM tag verifies. | `packages/archive/src/header.ts`, `packages/archive/src/extract.ts`                                                                                                                                                          | `packages/archive/src/*.test.ts` › tamper matrix (flip header byte, flip payload byte, swap chunks, drop chunk, truncate, append junk) — `<to be validated by the security gate>` | None known for confidentiality/integrity; a hostile file can still cost one Argon2id derivation (bounded) before rejection. |
| T3  | **Path traversal entry** (`../x`, `/etc/x`, `C:..`)                                              | A2       | Tar entries are filtered before node-tar's own checks: type allow-list (`File`, `Directory` only), no NUL, no `\`, no empty/`.`/`..` segments, no leading `/`, no drive prefix, bounded length and depth; every path must be `manifest.json`, `checksums.json` or under a `payloadPath` declared in the validated manifest; duplicates and case-insensitive collisions rejected. `Unpack` runs with `strict: true`, `preservePaths: false`, `strip: 0`, `maxDepth`. `isSafeArchivePath` and `safeJoin` guard the same rules elsewhere.                                                                                                                                                                                                            | `packages/archive/src/extract.ts`, `packages/shared/src/paths.ts` (`isSafeArchivePath`, `safeJoin`)                                                                                                        | `packages/shared/src/paths.test.ts` › `isSafeArchivePath` rejects `..`, absolute, drive letters, NUL; `packages/archive/src/extract.test.ts` › rejects traversal entries — `<to be validated by the security gate>`                         | None known.                                                                                                                                                                                                                  |
| T4  | **Symlink / hardlink attack** (link entry pointing outside; symlinked destination)               | A2, A3   | Link and hardlink tar entries are rejected outright (`ARCHIVE_ENTRY_REJECTED`) — the writer never produces them, so any link is evidence of tampering. Extraction targets a fresh 0700 `mkdtemp` directory. On the write side, `ScopedFs.assertAllowed` resolves the nearest existing ancestor with `realpath` and refuses when it leaves every approved root; `ScopedFs.copyDir` and `walkFiles` never follow symlinks (skipped and reported). Git provider materialises or skips symlinks before packing and records them in the manifest.                                                                                                                                                                                                      | `packages/shared/src/scoped-fs.ts`, `packages/shared/src/fs-utils.ts`, `packages/archive/src/extract.ts`                                                                                                   | `packages/shared/src/scoped-fs.test.ts` › symlink escape rejected with `PATH_OUTSIDE_ALLOWED_ROOT`; `packages/archive/src/extract.test.ts` › link entries rejected — `<to be validated by the security gate>`                               | A symlink created by another process between the `realpath` check and the write (TOCTOU) — mitigated by private staging and by writing into user-owned destinations only.                                                    |
| T5  | **Restore outside the approved destination**                                                     | A2, A3   | No destination is written before the user approves a `RestorePlan`. Providers restore through a `ScopedFs` whose roots are exactly the approved destinations (project `newPath`, Claude config dir, `~/.claude.json`). Any other target throws `PATH_OUTSIDE_ALLOWED_ROOT`; this is enforced by the facade, not by convention — providers have no raw `fs`. Collisions default to `skip`; destructive policies require an explicit per-collision choice.                                                                                                                                                                                                                                                                                          | `packages/shared/src/scoped-fs.ts`, `packages/core/src/restore/restore-engine.ts` (planned), `packages/core/src/providers/contract.ts` (`RestoreContext.fs`)                                               | `packages/shared/src/scoped-fs.test.ts` › writes outside roots rejected; `packages/core/src/**/migration.integration.test.ts` › restore writes only under approved roots — `<to be validated by the security gate>`                         | A provider could still overwrite files _inside_ an approved root if the user picks a destructive collision policy; the plan makes this explicit.                                                                             |
| T6  | **Corrupted archive** (bit rot, bad transfer, truncation)                                        | A2, A6   | Every chunk is authenticated; the first bad chunk stops decryption with `INTEGRITY_MISMATCH` and nothing after it is written. Truncation at any offset fails (last-chunk flag in the nonce; EOF asserted after the final chunk). `checksums.json` (SHA-256 + size per file) is verified after extraction. Backup re-reads and verifies the finished file before reporting success. A separate **Verify** job streams through a backup without extracting.                                                                                                                                                                                                                                                                                         | `packages/archive/src/extract.ts`, `packages/archive/src/verify.ts` (planned), `packages/core/src/integrity/integrity-verifier.ts` (planned)                                                               | `packages/archive/src/*.test.ts` › truncation and bit-flip cases; `packages/core/src/**/*.integration.test.ts` › backup verify phase — `<to be validated by the security gate>`                                                             | A corrupted backup is unrecoverable by design (no error-correction data). Keep two copies.                                                                                                                                   |
| T7  | **Decompression bomb / absurd sizes**                                                            | A2       | Hard limits from `DEFAULT_EXTRACTION_LIMITS` (`packages/archive/src/types.ts`): total plaintext ≤ 200 GiB, ≤ 2,000,000 entries, ≤ 50 GiB per entry, path length ≤ 1024. Entry sizes are additionally checked against the manifest's declared `sizeBytes`/`payloadBytes`; exceeding aborts with `ARCHIVE_LIMIT_EXCEEDED`. If compression is ever enabled, gunzip runs in our pipeline behind a byte-counting cap (node-tar's ratio guard only covers its own gunzip). Header length ≤ 64 KiB is rejected before allocation.                                                                                                                                                                                                                        | `packages/archive/src/types.ts`, `packages/archive/src/extract.ts`                                                                                                                                         | `packages/archive/src/extract.test.ts` › limit exceeded → `ARCHIVE_LIMIT_EXCEEDED` — `<to be validated by the security gate>`                                                                                                               | The defaults are generous by design (large monorepos); a user can still fill their own disk with a legitimate 150 GiB backup. Preflight checks free space.                                                                   |
| T8  | **Manipulated manifest** (bogus paths, sizes, provider ids, hints)                               | A2       | `manifest.json` must be the first tar entry and is parsed and zod-validated (`Manifest` schema) before any other entry is accepted. `payloadPath` values must be safe relative POSIX paths; every artifact's `sourcePath`/`meta` is treated as untrusted data by providers; `restoreHints` are free-form but only consulted for non-security decisions (e.g. directory-name encoding, which is re-verified on the destination). Provider `schemaVersion` mismatches are rejected or downgraded to a warning per provider.                                                                                                                                                                                                                         | `packages/model/src/manifest.ts`, `packages/archive/src/extract.ts`, provider `planRestore` implementations                                                                                                | `packages/model/src/*.test.ts` › manifest validation rejects malformed input; `packages/archive/src/extract.test.ts` › manifest-not-first rejected — `<to be validated by the security gate>`                                               | A syntactically valid manifest can still describe misleading labels; the plan shows real destinations, not labels, for every step.                                                                                           |
| T9  | **Renderer compromise**                                                                          | A4       | `sandbox`, `contextIsolation`, `nodeIntegration: false`, `webSecurity: true`; `will-navigate` blocked, `setWindowOpenHandler` denies (https forwarded to the OS browser); strict CSP (`default-src 'self'`, no remote origins); preload exposes only the fixed `window.devMigration` surface — no `ipcRenderer`, no generic invoke, no fs/exec. Main validates each request against the channel's zod schema, checks `event.senderFrame` (main frame of our window, expected origin), and dialogs run in main. Release builds flip fuses (`RunAsNode` off, `EnableNodeOptionsEnvironmentVariable` off, `EnableNodeCliInspectArguments` off, `OnlyLoadAppFromAsar` on). Secrets are redacted in main before anything reaches the renderer or logs. | `apps/desktop/src/main/index.ts`, `apps/desktop/src/preload/index.ts`, `apps/desktop/src/renderer/index.html` (CSP), `packages/ipc-contracts/src/channels.ts`, `apps/desktop/electron-builder.yml` (fuses) | `apps/desktop/src/main/**/*.test.ts` › untrusted sender rejected, malformed request rejected; `tests/e2e/*.e2e.ts` › `window.require`/`process` undefined, `window.open` denied — `<to be validated by the security gate>`                  | A compromised renderer can still call whitelisted channels with valid arguments (e.g. start a scan of a path it names); destructive operations still require the plan/approval flow and native dialogs.                      |
| T10 | **Malicious project file names** (`--flag`, newlines, NUL, `..`, very long, unicode confusables) | A3       | File names are never interpolated into commands; they are only ever copied via `ScopedFs` under a safe relative path (`safeJoin` rejects `..`, absolute, NUL). Git deltas use `-z` (NUL-separated) output so newlines in names cannot split records. Names are NFC-normalised for comparison and archive paths are checked with `isSafeArchivePath`; over-long paths are rejected at pack time and at extract time. Names are rendered as text in the UI (no HTML).                                                                                                                                                                                                                                                                               | `packages/shared/src/paths.ts`, `packages/shared/src/scoped-fs.ts`, `packages/providers/git/src/*` (planned), renderer components                                                                          | `packages/shared/src/paths.test.ts`; `packages/providers/git/src/*.integration.test.ts` › repo with hostile file names round-trips — `<to be validated by the security gate>`                                                               | Case-insensitive filesystem collisions between two distinct names are rejected rather than resolved.                                                                                                                         |
| T11 | **Command injection via git branch / path / remote strings**                                     | A3       | All subprocesses go through `Exec(file, args[])` (execa, `shell: false`); `child_process.exec` is banned by ESLint (`eslint.config.js`). Branch names are validated with `git check-ref-format` and rejected when they start with `-`; remote URLs and paths are validated and rejected when they start with `-`; `--` separators are used before positional arguments; `git` runs with an explicit `cwd` and minimal environment. Remote URLs are recorded as data and re-added with `git remote add -- <name> <url>` only after validation.                                                                                                                                                                                                     | `packages/shared/src/exec.ts`, `packages/providers/git/src/*` (planned), `eslint.config.js`                                                                                                                | `packages/providers/git/src/*.test.ts` › `-rf`, `--upload-pack=…`, ext:: style remotes rejected with `GIT_INVALID_REF`/`INVALID_INPUT` — `<to be validated by the security gate>`                                                           | `git` itself may interpret some URL schemes (e.g. `ext::`) if `protocol.*.allow` is permissive on the destination; the restore plan lists every remote before it is added.                                                   |
| T12 | **Cancellation mid-operation**                                                                   | A6       | Every job has an `AbortController`; long loops call `throwIfAborted`, streams use `pipeline(..., { signal })`, subprocesses receive `cancelSignal`. Backup writes to `<output>.part` in the output directory and renames on success only; on cancel the partial file and staging directory are removed and the report states that no source data was modified. Restore writes files atomically (temp + fsync + rename) so a cancelled job leaves either the old file or the complete new file, never a half-written one.                                                                                                                                                                                                                          | `packages/core/src/jobs/job-manager.ts`, `packages/shared/src/errors.ts` (`throwIfAborted`), `packages/shared/src/scoped-fs.ts` (`writeFileAtomic`), `packages/archive/src/create.ts`                      | `packages/core/src/jobs/*.test.ts` › cancel → `CANCELLED`; `packages/core/src/**/*.integration.test.ts` › cancelled backup leaves no `.part` — `<to be validated by the security gate>`                                                     | A restore cancelled after some providers finished is partial by nature; the report lists exactly which steps completed and the collision policy guarantees originals were kept (`skip`/`backup-then-replace`).               |
| T13 | **App crash with temp files**                                                                    | A5, A6   | Staging and extraction directories are created with `mkdtemp` and mode 0700 under the app's work dir (`makeTempDir`), so other local users cannot read plaintext. `RestoreEngine.cleanup()`/`CoreServices.dispose()` remove staging on quit; leftovers from a crash are removed on the next start (prefix `devmig-`). Partial archives are `.part` files with mode 0600 in the user's chosen output directory.                                                                                                                                                                                                                                                                                                                                    | `packages/shared/src/fs-utils.ts` (`makeTempDir`), `packages/core/src/api.ts` (`cleanup`, `dispose`), `apps/desktop/src/main` (startup sweep, planned)                                                     | `packages/shared/src/fs-utils.test.ts` › temp dir mode 0700; startup sweep test — `<to be validated by the security gate>`                                                                                                                  | Plaintext staging exists on disk for the duration of a job; a root-level attacker or forensic recovery could read it. FileVault mitigates at rest.                                                                           |
| T14 | **Claude Code running during restore** (concurrent writes to `~/.claude`, `~/.claude.json`)      | A6       | Preflight checks the live session registry (`<claudeConfigDir>/sessions/*.json`) and running `claude` processes and reports a **blocking** `CLAUDE_RUNNING` check; the user must quit Claude Code before the plan can proceed. Claude artifacts are merged add-only (sessions by id, `~/.claude.json` project entries, `history.jsonl` by `(sessionId, timestamp)`), written atomically, and a timestamped backup of `~/.claude.json` is kept in the same `backups/` convention Claude Code uses.                                                                                                                                                                                                                                                 | `packages/providers/claude-code/src/*` (planned) — preflight + restore, `packages/model/src/errors.ts` (`CLAUDE_RUNNING`)                                                                                  | `packages/providers/claude-code/src/*.integration.test.ts` › preflight blocks when a live session file exists — `<to be validated by the security gate>`                                                                                    | A session started _after_ preflight and _before_ writing wins the race; the atomic writes and add-only merges keep both sides consistent, and the retention sweep is age-based so it will not delete freshly restored files. |
| T15 | **Secrets leaking through logs, diagnostics, errors**                                            | A1, A5   | `createLogger` passes every message and context value through `redactSecrets`/`redactObject`; `serializeError` redacts messages, details and causes; `MachineInfo`, `ScannedArtifact.meta`, `restoreHints` and `summary` fields are documented as "no secrets". Diagnostics copy is built from redacted structures.                                                                                                                                                                                                                                                                                                                                                                                                                               | `packages/shared/src/redact.ts`, `packages/shared/src/logger.ts`, `packages/shared/src/errors.ts`                                                                                                          | `packages/shared/src/redact.test.ts` › known token shapes, `KEY=VALUE`, JWT, PEM redacted; `packages/shared/src/logger.test.ts` — `<to be validated by the security gate>`                                                                  | Redaction is pattern-based; an unusual secret format can slip through. Over-redaction is accepted.                                                                                                                           |
| T16 | **Credential migration by accident**                                                             | A1       | `Sensitivity = 'credential'` artifacts (Keychain, `oauthAccount`, session keys) are never selectable; `~/.claude.json` is never copied whole — only selected `projects[...]` entries are merged; `sensitive` artifacts are excluded by default and require explicit opt-in with a visible warning; the restore report's attention list tells the user to re-authenticate.                                                                                                                                                                                                                                                                                                                                                                         | `packages/model/src/artifacts.ts`, `packages/core/src/security/secret-classifier.ts` (planned), `packages/providers/claude-code/src/*` (planned)                                                           | `packages/core/src/security/*.test.ts` › credential never included; `packages/providers/claude-code/src/*.test.ts` › `oauthAccount` never emitted — `<to be validated by the security gate>`                                                | Transcripts may echo secrets in prose; they are classified `sensitive` when the redactor detects a token shape, but detection is heuristic. The backup is encrypted regardless.                                              |

## 5. Security gate checklist (before a release)

- [ ] Every `<to be validated by the security gate>` placeholder above names a real, passing test.
- [ ] `pnpm verify` and `pnpm verify:e2e` green on CI for the release commit.
- [ ] Tamper matrix from `docs/research/archive-crypto.md` §5.4 covered by tests.
- [ ] Hostile-archive fixture (traversal, links, oversize, manifest-not-first, duplicate/case-collision paths) rejected.
- [ ] Hostile-repository fixture (file names and branch names starting with `-`, containing newlines/NUL/unicode) round-trips or is rejected cleanly — never executed.
- [ ] `ScopedFs` escape tests (outside root, symlinked ancestor, `..`) pass.
- [ ] Packaged app: `npx @electron/fuses read --app "<app>"` matches the fuse table in ADR-0007; dev console shows no Electron security warnings; E2E asserts `window.require`/`window.process` are undefined.
- [ ] `grep -r "child_process" packages apps` shows only `packages/shared/src/exec.ts`-approved usage; ESLint `no-restricted-syntax` rules active.
- [ ] No secret-shaped strings in `Diagnostics → Copy` output produced against a fixture containing known tokens.
- [ ] Threat table rows re-read against the shipped code; residual risks updated.
